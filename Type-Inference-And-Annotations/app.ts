// Type Inference

// let age: number = 18;

let age = 'talhha'

//Type Annotations

let name: string = 'talhha'

let arr: [string, number] = ['talha', 18]

let union: string | number | boolean = true

function doSomething(a: string, b: number, c: boolean, d?:string): object {
    const something: object = {}
    return something
}